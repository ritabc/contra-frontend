// down the line, move end effects methods to this service, position methods to their own service, and move methods to their own service, thus cleaning up the animation component typescript. For now, it's nice to have them in one file

import { Injectable } from '@angular/core';

@Injectable()
